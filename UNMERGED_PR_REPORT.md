# UNMERGED_PR_REPORT.md

মার্জ **না হওয়া** ১৮টি PR (+১টি ইচ্ছাকৃত revert) প্রতিটি খুলে দেখা হয়েছে —
কী বদলাতে চেয়েছিল, এবং সেই কাজ আজকের `main`-এ আছে কি না।

পদ্ধতি: প্রতিটি PR-এর diff থেকে যোগ করা লাইন নিয়ে বর্তমান ট্রি-র সাথে মেলানো,
তারপর **ফিচার-স্তরে** যাচাই (রুট/ভিউ/সার্ভিস আসলে আছে কি না)। শুধু লাইন মেলানো
যথেষ্ট নয়, কারণ ফাইলগুলো পরে বহুবার নতুন করে লেখা হয়েছে।

---

## ক — পরে অন্য PR দিয়ে কাজটা হয়ে গেছে (ক্ষতি নেই)

| PR | কী চেয়েছিল | আজ কী অবস্থা |
|---|---|---|
| #3, #10 | গেম লবি রিডিজাইন | পরে #2, #26, #55, #60 মার্জ হয়েছে — লবি আছে |
| #15 | `routes/auth.js` সিনট্যাক্স এরর | #14 মার্জ হয়ে ঠিক হয়েছে |
| #16, #19, #20, #21 | অ্যাডমিন ইউজার এডিটিং + ম্যাচ সিঙ্ক | **আছে** — `routes/admin.js:1944, 2023` (`/users/:id`, `/users/:id/edit-profile`), `views/admin/user-detail.ejs`, `services/matchUpdater.js` |
| #24, #31 | DB আনরিচেবল হলে স্টার্টআপ ক্র্যাশ | **আছে** — `db.js`-এ `DATABASE_URL` না থাকলে warn করে চলে, ৫ বার রিট্রাই |
| #39 | মাইগ্রেশন `app.js` থেকে সরানো | **আছে** — আলাদা `migrations.js` |
| #50 | BN/EN বহুভাষা | #53, #80 দিয়ে হয়েছে — `locales/bn.json`, `locales/en.json` |
| #52 | পেমেন্ট/ওয়ালেট রিডিজাইন | #51 দিয়ে হয়েছে |
| #59 | প্রোফাইল রিডিজাইন | #58 দিয়ে হয়েছে |
| #30 | লাক্সারি গোল্ডেন থিম | #35, #41, #68 দিয়ে হয়েছে |
| #57 | Revert #56 | **ইচ্ছাকৃতভাবে মার্জ করা হয়নি** — ঠিক সিদ্ধান্ত, #56 রাখা হয়েছে |

**উপসংহার: এই ১৩টি PR হারানো কাজ নয়।**

---

## খ — কাজটা আজও অসম্পূর্ণ

### ১. গেম লবি ↔ গেম লজিক অমিল  (PR #18, #28 — মার্জ হয়নি)

PR #18 ৬২টি গেম, PR #28 ১১০+ গেম ও প্রোভাইডার যোগ করতে চেয়েছিল। দুটোই বাদ।
কিন্তু পরে #22, #25 দিয়ে গেম যোগ হয়েছে। আজকের অবস্থা:

```
routes/games.js  supportedGames : 118 টি গেম
routes/games.js  gameHandlers   :   7 টি আসল লজিক
views/index.ejs  লবিতে slug     :  47 টি
```

অর্থাৎ **১১১টি গেমের নিজস্ব সার্ভার লজিক নেই** — সেগুলো
`secureRandom.chance(0.45) ? betAmount * 2 : 0` fallback-এ চলে
(`routes/games.js:341`)। এটাই অডিট ফাইন্ডিং #10। লবিতে দেখানো ৪৭ আর
সমর্থিত ১১৮ — এই সংখ্যাও মেলে না।

**এটাই সবচেয়ে বড় অসম্পূর্ণ কাজ।**

### ২. রেজিস্ট্রেশনে ইমেইল OTP  (PR #28, #30 — মার্জ হয়নি)

দুটো PR-ই রেজিস্ট্রেশনে ইমেইল OTP চেয়েছিল। আজ:

- OTP আছে, কিন্তু **লগইনে VPN/প্রক্সি ধরা পড়লে step-up যাচাই** হিসেবে
  (`routes/auth.js:608, 625`)
- রেজিস্ট্রেশনে OTP নেই — সেখানে verification token সহ ইমেইল লিংক
  (`routes/auth.js:72, 319, 685`)

**সিদ্ধান্ত দরকার:** ইমেইল লিংক যথেষ্ট ধরে নিয়ে বন্ধ করা হবে, নাকি OTP
বসানো হবে। এখন যেটা আছে সেটাও কাজ করে, তাই এটা ঝুঁকি নয় — অসম্পূর্ণ ফিচার।

### ৩. স্পোর্টস অডস UI  (PR #44 — মার্জ হয়নি)

"Football Ticket" UI, decimal odds, payout calculator চেয়েছিল। আজ odds আছে
(`views/match-detail.ejs`-এ ১১ জায়গায়, `routes/matches.js`-এ ৩), কিন্তু PR-এর
পুরো UI আসেনি। অডিট ফাইন্ডিং #39, #40 (market↔match যাচাই, runner validation)
এখনো খোলা — অর্থাৎ **এই এলাকার কাজ অর্ধেক**।

### ৪. অব্যবহৃত CI workflow  (PR #3 — মার্জ হয়নি)

deno/gatsby/mdbook/static workflow যোগ করতে চেয়েছিল। মার্জ না হওয়া ঠিক
সিদ্ধান্ত — এই প্রজেক্টে ওগুলোর দরকার নেই। কিন্তু আসল CI সমস্যা
(ফাইন্ডিং #49–#51) রয়েই গেছে।

---

## গ — এক নজরে

| শ্রেণি | সংখ্যা |
|---|---|
| মার্জ না হওয়া PR | 18 (+১ ইচ্ছাকৃত revert) |
| পরে অন্যভাবে কাজ হয়ে গেছে | 13 |
| ইচ্ছাকৃতভাবে বাদ, ঠিক সিদ্ধান্ত | 2 (#57, #3) |
| **কাজ আজও অসম্পূর্ণ** | **3 (#18/#28 গেম লজিক, #28/#30 রেজিস্ট্রেশন OTP, #44 স্পোর্টস UI)** |

## সবচেয়ে জরুরি

**১১১টি গেমের আসল লজিক নেই।** ইউজার যে গেম খেলছে বলে ভাবছে, সার্ভার সেটা
খেলছে না — একটাই জেনেরিক ৪৫% হার/২x নিয়মে সব সেটেল হচ্ছে। এটা আসল টাকার
প্ল্যাটফর্মে product integrity সমস্যা, এবং মার্জ না হওয়া PR-গুলোর মধ্যে
এটাই একমাত্র বিষয় যেখানে কাজ সত্যিই বাকি আছে।

দুটো পথ:
- **ক)** প্রতিটি গেমের সার্ভার-সাইড লজিক লেখা (বড় কাজ, ১১১টি গেম)
- **খ)** যেগুলোর লজিক নেই সেগুলো লবিতে "শীঘ্রই আসছে" দেখিয়ে বন্ধ রাখা,
  তারপর ধাপে ধাপে চালু করা (ছোট কাজ, সৎ আচরণ)

**খ** আগে করা উচিত — কারণ এখন যা চলছে সেটা ইউজারকে ভুল দেখাচ্ছে।
