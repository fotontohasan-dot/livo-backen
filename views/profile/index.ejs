<%- include('../partials/head') %>

<style>
/* ──────────────────── Profile CSS ──────────────────── */
:root{--gold:#f5a623;--gold2:#ffd166;--gg:linear-gradient(135deg,#f5d67d,#f5a623,#d97706)}
.pf-wrap{max-width:520px;margin:0 auto;padding:0 0 110px;background:transparent}

/* Verify banner */
.pf-verify-banner{background:rgba(239,68,68,.1);border:1px solid rgba(239,68,68,.3);border-radius:14px;padding:12px 14px;margin:10px 12px;display:flex;flex-wrap:wrap;align-items:center;gap:8px;font-size:13px;color:rgba(255,255,255,.85)}
.pf-verify-text{flex:1;min-width:0}
.pf-verify-btn{background:rgba(239,68,68,.8);color:#fff;border:none;padding:6px 14px;border-radius:20px;font-size:12.5px;font-weight:700;cursor:pointer}

/* Header */
.pf-header{background:linear-gradient(160deg,#1a2235,#0d1117);padding:16px 14px 20px;position:relative;overflow:hidden}
.pf-header-glow{position:absolute;top:-60px;right:-40px;width:200px;height:200px;background:radial-gradient(circle,rgba(245,166,35,.18),transparent 70%);border-radius:50%;pointer-events:none}
.pf-header-top{display:flex;align-items:center;gap:14px;position:relative;z-index:2}
.pf-header-balance-row{display:flex;align-items:flex-end;justify-content:space-between;margin-top:18px;position:relative;z-index:2}
.pf-header-balance-label{color:rgba(255,255,255,.55);font-size:12px;font-weight:600}
.pf-header-balance-value{font-family:'Teko',sans-serif;font-size:34px;color:var(--gold);line-height:1;margin-top:2px}
.pf-header-refresh{background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);color:rgba(255,255,255,.7);width:36px;height:36px;border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.pf-quick-row.pf-header-quick-row{margin:16px 0 0}
.pf-avatar-wrap{position:relative;flex-shrink:0}
.pf-avatar{width:68px;height:68px;border-radius:50%;background:var(--gg);display:flex;align-items:center;justify-content:center;font-size:26px;font-weight:900;color:#000;border:3px solid rgba(245,166,35,.5);box-shadow:0 0 20px rgba(245,166,35,.25);overflow:hidden;cursor:pointer;background-size:cover;background-position:center}
.pf-avatar-edit-btn{position:absolute;bottom:-2px;right:-2px;background:#111;border:2px solid var(--gold);border-radius:50%;width:24px;height:24px;display:flex;align-items:center;justify-content:center;font-size:10px;cursor:pointer;color:var(--gold)}
.pf-online-dot{position:absolute;top:2px;left:2px;width:12px;height:12px;background:#22c55e;border-radius:50%;border:2px solid #0d1117;animation:pdot 2s infinite}
@keyframes pdot{0%,100%{box-shadow:0 0 0 0 rgba(34,197,94,.4)}50%{box-shadow:0 0 0 5px rgba(34,197,94,0)}}
.pf-user-info{flex:1;min-width:0}
.pf-username-row{display:flex;align-items:center;gap:6px;flex-wrap:wrap}
.pf-username{color:#fff;font-size:18px;font-weight:800}
.pf-vip-badge-sm{background:var(--gg);color:#3a1e00;font-size:10.5px;padding:2px 8px;border-radius:20px;font-weight:900;white-space:nowrap;text-decoration:none}
.pf-user-id{color:rgba(255,255,255,.4);font-size:11.5px;margin-top:3px;display:flex;align-items:center;gap:5px}
.pf-copy-id-btn{background:none;border:none;color:var(--gold);cursor:pointer;padding:0;font-size:11px}
.pf-notif-btn{position:relative;background:rgba(255,255,255,.08);border:none;width:42px;height:42px;border-radius:50%;display:flex;align-items:center;justify-content:center;color:rgba(255,255,255,.8);font-size:17px;cursor:pointer;flex-shrink:0;text-decoration:none}
.pf-notif-dot{position:absolute;top:4px;right:4px;background:#e53935;color:#fff;font-size:9px;font-weight:800;min-width:17px;height:17px;border-radius:50%;display:flex;align-items:center;justify-content:center;border:1.5px solid #0d1117;padding:0 2px}

/* Profile completion */
.pf-completion{margin:10px 0 0;padding:8px 12px;background:rgba(255,255,255,.05);border-radius:10px;display:flex;align-items:center;gap:10px;font-size:12px;color:rgba(255,255,255,.6)}
.pf-comp-bar{flex:1;height:5px;background:rgba(255,255,255,.1);border-radius:10px;overflow:hidden}
.pf-comp-fill{height:100%;background:var(--gg);border-radius:10px}

/* Cards */
.pf-card{background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.08);border-radius:20px;margin:12px 12px 0;overflow:hidden}
.pf-card-title-row{display:flex;align-items:center;justify-content:space-between;padding:14px 16px 10px}
.pf-card-title{color:#fff;font-size:15px;font-weight:800;display:flex;align-items:center;gap:7px}
.pf-mini-btn{background:rgba(245,166,35,.15);color:var(--gold);border:1px solid rgba(245,166,35,.3);border-radius:20px;padding:4px 12px;font-size:12px;font-weight:700;text-decoration:none;white-space:nowrap}

/* VIP */
.pf-vip-card{}
.pf-vip-body{padding:0 16px 14px}
.pf-vip-progress-bar{height:7px;background:rgba(255,255,255,.1);border-radius:10px;overflow:hidden;margin-bottom:7px}
.pf-vip-progress-fill{height:100%;background:var(--gg);border-radius:10px;transition:width .8s}
.pf-vip-info{color:rgba(255,255,255,.5);font-size:12px}
.pf-vip-info b{color:rgba(255,255,255,.8)}
.pf-wallet-mini{display:flex;justify-content:space-between;margin-top:8px;padding-top:8px;border-top:1px solid rgba(255,255,255,.06)}
.pf-wm-label{color:rgba(255,255,255,.4);font-size:12px}
.pf-wm-value{color:var(--gold);font-size:12px;font-weight:700}

/* Wallet */
.pf-wallet-card{}
.pf-wallet-balance-row{padding:0 16px 12px;display:flex;align-items:flex-end;justify-content:space-between}
.pf-balance-amount{color:var(--gold);font-size:30px;font-weight:800;font-family:'Teko',sans-serif;line-height:1.1}
.pf-balance-label{color:rgba(255,255,255,.4);font-size:11px;margin-bottom:4px}
.pf-refresh-btn{background:rgba(245,166,35,.12);border:none;width:38px;height:38px;border-radius:50%;display:flex;align-items:center;justify-content:center;cursor:pointer;color:var(--gold);font-size:15px;transition:background .2s}
.pf-refresh-btn:active{background:rgba(245,166,35,.3)}
.pf-wallet-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;padding:0 16px 14px}
.pf-wallet-item{background:rgba(255,255,255,.04);border-radius:12px;padding:9px 12px;border:1px solid rgba(255,255,255,.05)}
.pf-wi-label{color:rgba(255,255,255,.38);font-size:10.5px;margin-bottom:2px}
.pf-wi-value{color:#fff;font-size:13px;font-weight:700}
.pf-wi-value.demo{color:var(--gold)}
.pf-last-updated{color:rgba(255,255,255,.3);font-size:11px;padding:0 16px 10px}

/* Hub Quick Actions */
.pf-hub-actions{display:grid;grid-template-columns:repeat(4,1fr);gap:0;border-top:1px solid rgba(255,255,255,.06);margin:0 12px}
.pf-hub-actions.card-style{background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.08);border-radius:20px;margin:12px 12px 0;overflow:hidden;padding:4px}
.pf-qa{display:flex;flex-direction:column;align-items:center;gap:6px;padding:14px 4px;text-decoration:none;transition:background .15s;border-radius:16px}
.pf-qa:active{background:rgba(255,255,255,.06)}
.pf-qa i{font-size:20px}
.pf-qa span{color:rgba(255,255,255,.75);font-size:11.5px;font-weight:600;text-align:center;line-height:1.3}
.pf-qa.deposit i{color:#22c55e}.pf-qa.withdraw i{color:#f87171}.pf-qa.cards i{color:#818cf8}.pf-qa.history i{color:#2dd4bf}
.pf-qa.vip i{color:#c084fc}.pf-qa.reward i{color:var(--gold)}.pf-qa.security i{color:#38bdf8}.pf-qa.more i{color:rgba(255,255,255,.5)}

/* Menu */
.pf-menu-card{background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.08);border-radius:20px;margin:12px 12px 0;overflow:hidden}

/* Quick Actions (Deposit/Withdraw/My Cards) — সরাসরি Wallet Card এর নিচে, একবারে দৃশ্যমান */
.pf-quick-row{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:12px 12px 0}
.pf-quick-btn{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:6px;padding:14px 6px;border-radius:16px;text-decoration:none;background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.08);transition:transform .12s,background .15s}
.pf-quick-btn:active{transform:scale(.96);background:rgba(255,255,255,.08)}
.pf-quick-btn i{font-size:19px}
.pf-quick-btn span{color:rgba(255,255,255,.8);font-size:12px;font-weight:700}
.pf-quick-btn.deposit i{color:#22c55e}
.pf-quick-btn.withdraw i{color:#f87171}
.pf-quick-btn.cards i{color:#818cf8}

/* Member Center — ৫টা বড় হাব টাইল (Wallet/VIP/Reward/Account/Support), ট্যাপ করলে সাব-আইটেম প্যানেল খোলে */
.mc-grid-card{background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.08);border-radius:var(--radius-lg, 20px);margin:14px 12px 0;overflow:hidden}
.mc-grid-title{padding:14px 16px 8px;color:#fff;font-size:15px;font-weight:800;display:flex;align-items:center;gap:7px}
.mc-hub-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:2px;padding:6px 8px 10px}
.mc-hub-tile{position:relative;display:flex;flex-direction:column;align-items:center;justify-content:flex-start;gap:7px;padding:14px 4px;text-decoration:none;border-radius:16px;transition:background .15s;background:none;border:none;font-family:inherit;cursor:pointer}
.mc-hub-tile:active{background:rgba(255,255,255,.06)}
.mc-hub-tile.active{background:rgba(234,179,8,.1)}
.mc-hub-tile span:last-child{color:rgba(255,255,255,.75);font-size:11.5px;font-weight:700;text-align:center;line-height:1.25}
.mc-grid-badge{position:absolute;top:6px;right:14px;background:#e53935;color:#fff;font-size:9px;font-weight:800;min-width:16px;height:16px;border-radius:9px;display:flex;align-items:center;justify-content:center;padding:0 4px;border:1.5px solid #12121f}
.mc-grid-badge.amber{background:#f59e0b}
.mc-hub-panel{display:none;grid-template-columns:repeat(2,1fr);gap:2px;padding:4px 8px 12px;border-top:1px solid rgba(255,255,255,.06);background:rgba(255,255,255,.015)}
.mc-hub-panel.open{display:grid}
.ic-sky-txt{color:#38bdf8}
.mc-inline-badge{background:#e53935;color:#fff;font-size:10px;font-weight:800;min-width:16px;height:16px;border-radius:8px;display:inline-flex;align-items:center;justify-content:center;padding:0 4px}
.mc-inline-badge.amber{background:#f59e0b}

/* Recent Activity */
.pf-activity-card{background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.08);border-radius:20px;margin:12px 12px 0;overflow:hidden}
.pf-activity-list{padding:0 8px 8px}
.pf-activity-row{display:flex;align-items:center;gap:12px;padding:10px 8px;border-bottom:1px solid rgba(255,255,255,.04)}
.pf-activity-row:last-child{border-bottom:none}
.pf-activity-icon{width:36px;height:36px;border-radius:10px;display:flex;align-items:center;justify-content:center;font-size:14px;flex-shrink:0}
.pf-activity-body{flex:1;min-width:0}
.pf-activity-title{color:rgba(255,255,255,.85);font-size:12.5px;font-weight:600}
.pf-activity-time{color:rgba(255,255,255,.35);font-size:10.5px;margin-top:1px}
.pf-activity-amount{font-size:12.5px;font-weight:700;white-space:nowrap;flex-shrink:0}
.pf-activity-empty{padding:24px 16px;text-align:center;color:rgba(255,255,255,.3);font-size:12.5px}

/* Footer */
.pf-footer{text-align:center;padding:22px 16px 10px;color:rgba(255,255,255,.28);font-size:11px}
.pf-footer .ver{margin-top:4px;color:rgba(255,255,255,.2)}

.mc-section-label{padding:12px 16px 6px;color:rgba(255,255,255,.35);font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.7px;display:flex;align-items:center;gap:6px}
.mc-section-label i{color:var(--gold);font-size:12px}
.mc-hub-item{display:flex;align-items:center;justify-content:space-between;padding:12px 16px;border-bottom:1px solid rgba(255,255,255,.04);text-decoration:none;transition:background .15s;cursor:pointer}
.mc-hub-item:last-child{border-bottom:none}
.mc-hub-item:active{background:rgba(255,255,255,.04)}
.mc-hub-left{display:flex;align-items:center;gap:12px}
.mc-hub-icon{width:38px;height:38px;border-radius:12px;display:flex;align-items:center;justify-content:center;font-size:16px;flex-shrink:0}
.mc-hub-label{color:rgba(255,255,255,.85);font-size:13.5px;font-weight:600}
.mc-hub-sub{color:rgba(255,255,255,.35);font-size:11px;margin-top:1px}
.mc-hub-right{display:flex;align-items:center;gap:6px}
.mc-hub-badge{background:#e53935;color:#fff;font-size:10px;font-weight:800;min-width:20px;height:20px;border-radius:10px;display:flex;align-items:center;justify-content:center;padding:0 5px}
.mc-hub-badge.amber{background:#f59e0b}
.mc-hub-arrow{color:rgba(255,255,255,.2);font-size:11px}

/* Icon color presets */
.ic-gold{background:rgba(245,166,35,.15);color:var(--gold)}
.ic-green{background:rgba(34,197,94,.12);color:#22c55e}
.ic-red{background:rgba(239,68,68,.12);color:#f87171}
.ic-blue{background:rgba(99,102,241,.15);color:#818cf8}
.ic-purple{background:rgba(168,85,247,.12);color:#c084fc}
.ic-teal{background:rgba(20,184,166,.12);color:#2dd4bf}
.ic-orange{background:rgba(249,115,22,.12);color:#fb923c}
.ic-sky{background:rgba(14,165,233,.12);color:#38bdf8}

/* Logout */
.pf-logout-row{margin:16px 12px 0;display:flex;align-items:center;gap:12px;padding:14px 16px;background:rgba(239,68,68,.08);border:1px solid rgba(239,68,68,.15);border-radius:20px;text-decoration:none;transition:background .15s}
.pf-logout-row:active{background:rgba(239,68,68,.14)}
.pf-logout-icon{width:38px;height:38px;border-radius:12px;background:rgba(239,68,68,.12);color:#f87171;display:flex;align-items:center;justify-content:center;font-size:16px;flex-shrink:0}
.pf-logout-label{color:#f87171;font-size:14px;font-weight:700}
.pf-version{text-align:center;margin-top:16px;margin-bottom:4px;color:rgba(255,255,255,.2);font-size:11px}

/* Sticky deposit FAB */
.pf-sticky-deposit{position:fixed;bottom:80px;right:16px;background:var(--gg);color:#000;width:52px;height:52px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:22px;box-shadow:0 4px 16px rgba(245,166,35,.4);text-decoration:none;z-index:100;transition:transform .15s}
.pf-sticky-deposit:active{transform:scale(.93)}

/* Toast */
#pf-toast{position:fixed;bottom:100px;left:50%;transform:translateX(-50%) translateY(20px);background:#1c2533;color:#fff;padding:9px 20px;border-radius:30px;font-size:12.5px;font-weight:600;border:1px solid var(--gold);opacity:0;transition:all .25s;z-index:9999;pointer-events:none;white-space:nowrap}

/* Spinning */
.spinning{animation:sp360 .7s linear infinite}
@keyframes sp360{to{transform:rotate(360deg)}}

/* Expandable hub items */
.mc-expandable{border-bottom:1px solid rgba(255,255,255,.05)}
.mc-expandable:last-of-type{border-bottom:none}
.mc-hub-summary{display:flex;align-items:center;gap:12px;padding:13px 16px;cursor:pointer;list-style:none;transition:background .15s;outline:none}
.mc-hub-summary::-webkit-details-marker{display:none}
.mc-hub-summary:active{background:rgba(255,255,255,.04)}
details[open] .mc-hub-summary .mc-hub-chev{transform:rotate(90deg)}
.mc-hub-icon{width:38px;height:38px;border-radius:12px;display:flex;align-items:center;justify-content:center;font-size:15px;flex-shrink:0}
.mc-hub-label{flex:1;color:rgba(255,255,255,.88);font-size:13.5px;font-weight:700}
.mc-hub-chev{color:rgba(255,255,255,.25);font-size:11px;transition:transform .2s}
.mc-sub-body{background:rgba(255,255,255,.02);border-top:1px solid rgba(255,255,255,.04);padding:4px 0 6px}
.mc-sub-item{display:flex;align-items:center;gap:10px;padding:11px 16px 11px 24px;color:rgba(255,255,255,.72);font-size:13px;font-weight:500;text-decoration:none;transition:background .15s}
.mc-sub-item:hover,.mc-sub-item:active{background:rgba(255,255,255,.05)}
.mc-sub-item i{font-size:14px;width:18px;text-align:center;flex-shrink:0}

/* Icon text colors */
.ic-gold{background:rgba(245,166,35,.15);color:var(--gold)}
.ic-purple{background:rgba(168,85,247,.12);color:#c084fc}
.ic-orange{background:rgba(249,115,22,.12);color:#fb923c}
.ic-sky{background:rgba(14,165,233,.12);color:#38bdf8}
.ic-teal{background:rgba(20,184,166,.12);color:#2dd4bf}
.ic-gold-txt{color:var(--gold)}
.ic-green-txt{color:#22c55e}
.ic-red-txt{color:#f87171}
.ic-blue-txt{color:#818cf8}
.ic-teal-txt{color:#2dd4bf}
.ic-purple-txt{color:#c084fc}
.ic-orange-txt{color:#fb923c}
.ic-pink-txt{color:#f472b6}


/* Light mode */
body.light-mode .pf-header{background:linear-gradient(160deg,#e5e7eb,#d1d5db)}
body.light-mode .pf-username{color:#111}
body.light-mode .pf-card{background:rgba(0,0,0,.04);border-color:rgba(0,0,0,.08)}
body.light-mode .mc-hub-label{color:#374151}
body.light-mode .mc-hub-sub{color:#9ca3af}
body.light-mode .pf-vip-info{color:#6b7280}
body.light-mode .pf-wi-label{color:#9ca3af}
body.light-mode .pf-wi-value{color:#111}
body.light-mode .pf-balance-label{color:#9ca3af}
</style>

  <%- include('../partials/flash') %>

  <% if (user.email && !user.email_verified) { %>
  <div class="pf-verify-banner">
    <i class="fas fa-triangle-exclamation"></i>
    <div class="pf-verify-text">আপনার ইমেইল এখনো ভেরিফাই করা হয়নি — কিছু ফিচার (যেমন উইথড্র) ব্যবহার করতে ভেরিফিকেশন লাগবে।</div>
    <form action="/resend-verification" method="POST" style="margin:0;">
      <button type="submit" class="pf-verify-btn">লিঙ্ক পাঠান</button>
    </form>
  </div>
  <% } %>

  <!-- ==================== 1. UNIFIED HEADER CARD (Avatar/ID/VIP badge/Bell + Balance + Deposit/Withdraw/Cards) ==================== -->
  <div class="pf-header">
    <div class="pf-header-glow"></div>
    <div class="pf-header-top">
      <div class="pf-avatar-wrap">
        <div id="avatarCircle" onclick="openAvatarPicker()" class="pf-avatar" style="background-image: <%= user.avatar ? `url('${user.avatar}')` : 'none' %>;">
          <% if (!user.avatar) { %><%= user.username.charAt(0).toUpperCase() %><% } %>
        </div>
        <span class="pf-online-dot" title="অনলাইন"></span>
        <div onclick="openAvatarPicker()" class="pf-avatar-edit">✏️</div>
      </div>

      <div class="pf-id-block">
        <div class="pf-username-row">
          <span id="usernameText" class="pf-username"><%= user.username %></span>
          <button onclick="copyUsername()" title="কপি করুন" class="pf-copy-btn"><i class="fas fa-copy"></i></button>
          <a href="/profile/vip" class="pf-vip-badge-sm">
            <i class="fas fa-crown"></i><%= (vipStatus && vipStatus.current) ? vipStatus.current.name : 'VIP' %>
          </a>
        </div>
        <div class="pf-uid-row">
          <span>ID: <%= 100000 + user.id %></span>
          <button onclick="copyUid()" title="কপি করুন" class="pf-copy-btn pf-copy-uid"><i class="fas fa-copy"></i></button>
        </div>
      </div>

      <a href="/notifications" class="pf-notif-icon">
        <i class="fas fa-bell"></i>
        <% if (typeof notifPreview !== 'undefined' && notifPreview.unreadCount > 0) { %><span class="pf-notif-dot"></span><% } %>
      </a>
    </div>

    <!-- Total Balance (Wallet Card বিলুপ্ত, এখানেই একীভূত) -->
    <div class="pf-header-balance-row">
      <div>
        <div class="pf-header-balance-label"><%= t.total_balance %></div>
        <div id="balanceText" class="pf-header-balance-value">৳ <%= Number(user.coins).toLocaleString(lang === 'bn' ? 'bn-BD' : 'en-US') %></div>
        <div class="pf-wallet-updated">সর্বশেষ আপডেট: <span id="lastUpdatedText">এখনই</span></div>
      </div>
      <button onclick="refreshBalance()" id="refreshBtn" title="রিফ্রেশ" class="pf-icon-btn pf-header-refresh"><i class="fas fa-sync-alt" id="refreshIcon"></i></button>
    </div>

    <!-- Deposit / Withdraw / My Cards — হেডার কার্ডেই, আলাদা Wallet Card নেই -->
    <div class="pf-quick-row pf-header-quick-row">
      <a href="/payment/deposit" class="pf-quick-btn deposit"><i class="fas fa-arrow-down-circle"></i><span>ডিপোজিট</span></a>
      <a href="/payment/withdraw" class="pf-quick-btn withdraw"><i class="fas fa-arrow-up-circle"></i><span>উইথড্র</span></a>
      <a href="/profile/cards" class="pf-quick-btn cards"><i class="fas fa-credit-card"></i><span>আমার কার্ড</span></a>
    </div>
  </div>

  <!-- ==================== PROFILE COMPLETION ==================== -->
  <% if (typeof profileCompletion !== 'undefined') { %>
  <div class="pf-completion-row">
    <div class="pf-completion-track"><div class="pf-completion-fill" style="width: <%= profileCompletion %>%;"></div></div>
    <span class="pf-completion-text">প্রোফাইল সম্পূর্ণতা <b><%= profileCompletion %>%</b></span>
  </div>
  <% } %>

  <!-- ==================== 2. MEMBER CENTER — Wallet Hub / VIP Center / Reward Center / Account Center / Support Center (৫টা বড় গ্রিড টাইল) ==================== -->
  <div class="mc-grid-card">
    <div class="mc-grid-title"><i class="fas fa-grid-2" style="color:var(--gold);"></i> মেম্বার সেন্টার</div>
    <div class="mc-hub-grid">
      <button type="button" onclick="toggleHub('wallet')" data-hub="wallet" class="mc-hub-tile">
        <span class="mc-hub-icon" style="background:rgba(234,179,8,.15);color:var(--gold);"><i class="fas fa-wallet"></i></span>
        <span>ওয়ালেট হাব</span>
      </button>
      <button type="button" onclick="toggleHub('vip')" data-hub="vip" class="mc-hub-tile">
        <span class="mc-hub-icon" style="background:rgba(192,132,252,.15);color:#c084fc;"><i class="fas fa-crown"></i></span>
        <span>VIP সেন্টার</span>
      </button>
      <button type="button" onclick="toggleHub('reward')" data-hub="reward" class="mc-hub-tile">
        <% if ((typeof rewardBadge !== 'undefined' && rewardBadge > 0) || (typeof missionBadge !== 'undefined' && missionBadge > 0)) { %>
        <span class="mc-grid-badge"><%= (rewardBadge||0) + (missionBadge||0) %></span>
        <% } %>
        <span class="mc-hub-icon" style="background:rgba(245,166,35,.15);color:var(--gold);"><i class="fas fa-gift"></i></span>
        <span>রিওয়ার্ড সেন্টার</span>
      </button>
      <button type="button" onclick="toggleHub('account')" data-hub="account" class="mc-hub-tile">
        <% if (typeof security !== 'undefined' && security && (security.score || 0) < 60) { %>
        <span class="mc-grid-badge amber">!</span>
        <% } %>
        <span class="mc-hub-icon" style="background:rgba(56,189,248,.15);color:#38bdf8;"><i class="fas fa-user-shield"></i></span>
        <span>অ্যাকাউন্ট সেন্টার</span>
      </button>
      <button type="button" onclick="toggleHub('support')" data-hub="support" class="mc-hub-tile">
        <% if (typeof notifPreview !== 'undefined' && notifPreview.unreadCount > 0) { %>
        <span class="mc-grid-badge"><%= notifPreview.unreadCount %></span>
        <% } %>
        <span class="mc-hub-icon" style="background:rgba(34,197,94,.15);color:#22c55e;"><i class="fas fa-headset"></i></span>
        <span>সাপোর্ট সেন্টার</span>
      </button>
      <a href="/logout" class="mc-hub-tile" style="text-decoration:none;">
        <span class="mc-hub-icon" style="background:rgba(255,255,255,.08);color:rgba(255,255,255,.6);"><i class="fas fa-right-from-bracket"></i></span>
        <span>লগআউট</span>
      </a>
    </div>

    <!-- Wallet Hub sub-items -->
    <div id="hub-wallet" class="mc-hub-panel">
      <a href="/payment/deposit" class="mc-sub-item"><i class="fas fa-arrow-down-circle ic-green-txt"></i>ডিপোজিট</a>
      <a href="/payment/withdraw" class="mc-sub-item"><i class="fas fa-arrow-up-circle ic-red-txt"></i>উইথড্র</a>
      <a href="/profile/cards" class="mc-sub-item"><i class="fas fa-credit-card ic-blue-txt"></i>আমার কার্ড</a>
      <a href="/profile/transactions" class="mc-sub-item"><i class="fas fa-clock-rotate-left ic-teal-txt"></i>ট্রানজেকশন হিস্টোরি</a>
    </div>

    <!-- VIP Center sub-items -->
    <div id="hub-vip" class="mc-hub-panel">
      <a href="/profile/vip" class="mc-sub-item"><i class="fas fa-chart-line ic-purple-txt"></i>VIP প্রগ্রেস</a>
      <a href="/profile/vip" class="mc-sub-item"><i class="fas fa-gift ic-gold-txt"></i>VIP রিওয়ার্ড</a>
      <a href="/profile/vip" class="mc-sub-item"><i class="fas fa-history ic-teal-txt"></i>VIP হিস্টোরি</a>
      <a href="/profile/vip" class="mc-sub-item"><i class="fas fa-star ic-gold-txt"></i>VIP সুবিধা</a>
    </div>

    <!-- Reward Center sub-items -->
    <div id="hub-reward" class="mc-hub-panel">
      <a href="/profile/rewards" class="mc-sub-item"><i class="fas fa-calendar-day ic-orange-txt"></i>ডেইলি বোনাস</a>
      <a href="/profile/rewards" class="mc-sub-item"><i class="fas fa-calendar-week ic-orange-txt"></i>উইকলি বোনাস</a>
      <a href="/profile/rewards" class="mc-sub-item"><i class="fas fa-calendar ic-orange-txt"></i>মান্থলি বোনাস</a>
      <a href="/profile/wheel" class="mc-sub-item"><i class="fas fa-circle-dot ic-pink-txt"></i>লাকি স্পিন</a>
      <a href="/profile/wheel" class="mc-sub-item"><i class="fas fa-egg ic-gold-txt"></i>গোল্ডেন এগ</a>
      <a href="/profile/cashback" class="mc-sub-item"><i class="fas fa-coins ic-gold-txt"></i>ক্যাশব্যাক</a>
      <a href="/profile/rewards" class="mc-sub-item"><i class="fas fa-ticket ic-teal-txt"></i>কুপন</a>
      <a href="/profile/missions" class="mc-sub-item"><i class="fas fa-flag-checkered ic-teal-txt"></i>মিশন</a>
      <a href="/profile/badges" class="mc-sub-item"><i class="fas fa-trophy ic-gold-txt"></i>অ্যাচিভমেন্ট</a>
    </div>

    <!-- Account Center sub-items -->
    <div id="hub-account" class="mc-hub-panel">
      <a href="/profile/security" class="mc-sub-item"><i class="fas fa-user-circle ic-blue-txt"></i>আমার প্রোফাইল</a>
      <a href="/profile/security" class="mc-sub-item"><i class="fas fa-shield-halved ic-blue-txt"></i>সিকিউরিটি সেন্টার</a>
      <a href="/profile/login-history" class="mc-sub-item"><i class="fas fa-clock-rotate-left ic-purple-txt"></i>লগইন হিস্টোরি</a>
      <a href="/profile/security" class="mc-sub-item"><i class="fas fa-chart-line ic-sky-txt"></i>সাম্প্রতিক অ্যাক্টিভিটি</a>
      <a href="/profile/security" class="mc-sub-item"><i class="fas fa-mobile-screen ic-pink-txt"></i>ট্রাস্টেড ডিভাইস</a>
      <a href="/extra/kyc" class="mc-sub-item"><i class="fas fa-id-card ic-green-txt"></i>KYC
        <% if (typeof security !== 'undefined' && security && security.kycStatus !== 'approved') { %><span class="mc-inline-badge amber" style="margin-left:4px;font-size:9px">!</span><% } %>
      </a>
    </div>

    <!-- Support Center sub-items -->
    <div id="hub-support" class="mc-hub-panel">
      <a href="/profile/chat" class="mc-sub-item"><i class="fas fa-headset ic-green-txt"></i>কাস্টমার সার্ভিস</a>
      <a href="/help-center" class="mc-sub-item"><i class="fas fa-circle-question ic-blue-txt"></i>হেল্প সেন্টার</a>
      <a href="/profile/feedback" class="mc-sub-item"><i class="fas fa-comment-dots ic-orange-txt"></i>পরামর্শ</a>
      <a href="/notifications" class="mc-sub-item"><i class="fas fa-envelope ic-purple-txt"></i>ইন্টারনাল মেসেজ</a>
    </div>
  </div>

  <!-- ==================== 6. RECENT ACTIVITY ==================== -->
  <div class="pf-activity-card">
    <div class="pf-card-title-row">
      <div class="pf-card-title"><i class="fas fa-clock-rotate-left" style="color:var(--gold);"></i> সাম্প্রতিক অ্যাক্টিভিটি</div>
    </div>
    <div class="pf-activity-list">
      <% if (typeof recentActivity !== 'undefined' && recentActivity && recentActivity.length > 0) { %>
        <% recentActivity.forEach(function(act){
             var iconMap = { login: ['fa-right-to-bracket', '#38bdf8'], deposit: ['fa-arrow-down-circle', '#22c55e'], withdraw: ['fa-arrow-up-circle', '#f87171'], vip_upgrade: ['fa-crown', '#c084fc'], reward_claim: ['fa-gift', 'var(--gold)'] };
             var meta = iconMap[act.kind] || ['fa-circle-dot', 'rgba(255,255,255,.5)'];
             var labelMap = { login: 'লগইন', deposit: 'ডিপোজিট', withdraw: 'উইথড্র', vip_upgrade: 'VIP আপগ্রেড', reward_claim: 'রিওয়ার্ড ক্লেইম' };
             var label = labelMap[act.kind] || act.kind;
        %>
        <div class="pf-activity-row">
          <span class="pf-activity-icon" style="background:<%= meta[1] %>22;color:<%= meta[1] %>;"><i class="fas <%= meta[0] %>"></i></span>
          <div class="pf-activity-body">
            <div class="pf-activity-title"><%= label %></div>
            <div class="pf-activity-time"><%= new Date(act.created_at).toLocaleString(lang === 'bn' ? 'bn-BD' : 'en-US', { day:'numeric', month:'short', hour:'2-digit', minute:'2-digit' }) %></div>
          </div>
          <% if (act.amount !== null && act.amount !== undefined) { %>
          <div class="pf-activity-amount" style="color:<%= act.kind === 'withdraw' ? '#f87171' : '#22c55e' %>;">৳<%= Number(act.amount).toLocaleString(lang === 'bn' ? 'bn-BD' : 'en-US') %></div>
          <% } %>
        </div>
        <% }); %>
      <% } else { %>
        <div class="pf-activity-empty">এখনো কোনো অ্যাক্টিভিটি নেই</div>
      <% } %>
    </div>
  </div>

  <!-- ==================== 7. FOOTER ==================== -->
  <div class="pf-footer">
    <div>© <%= new Date().getFullYear() %> LIVO — সব অধিকার সংরক্ষিত</div>
    <div class="ver">v2.2.0</div>
  </div>


<script>
function toggleHub(name) {
  var panel = document.getElementById('hub-' + name);
  var tile = document.querySelector('.mc-hub-tile[data-hub="' + name + '"]');
  var isOpen = panel && panel.classList.contains('open');
  document.querySelectorAll('.mc-hub-panel.open').forEach(function (p) { p.classList.remove('open'); });
  document.querySelectorAll('.mc-hub-tile.active').forEach(function (t) { t.classList.remove('active'); });
  if (!isOpen && panel) {
    panel.classList.add('open');
    if (tile) tile.classList.add('active');
    panel.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }
}

function showToast(msg) {
  var el = document.getElementById('copyToast');
  el.textContent = msg;
  el.style.opacity = '1';
  el.style.transform = 'translateX(-50%) translateY(0)';
  setTimeout(function () {
    el.style.opacity = '0';
    el.style.transform = 'translateX(-50%) translateY(20px)';
  }, 1800);
}

function copyUsername() {
  var name = document.getElementById('usernameText').textContent.trim();
  copyText(name);
}

function copyUid() {
  copyText('<%= 100000 + user.id %>');
}

function copyText(val) {
  if (navigator.clipboard && navigator.clipboard.writeText) {
    navigator.clipboard.writeText(val).then(function () { showToast('✅ কপি হয়েছে!'); });
  } else {
    var ta = document.createElement('textarea');
    ta.value = val;
    document.body.appendChild(ta);
    ta.select();
    document.execCommand('copy');
    document.body.removeChild(ta);
    showToast('✅ কপি হয়েছে!');
  }
}

var AVATAR_OPTIONS = [
  'https://i.pravatar.cc/300?img=12',
  'https://i.pravatar.cc/300?img=33',
  'https://i.pravatar.cc/300?img=5',
  'https://i.pravatar.cc/300?img=47',
  'https://i.pravatar.cc/300?img=8',
  'https://i.pravatar.cc/300?img=25',
  'https://i.pravatar.cc/300?img=15',
  'https://i.pravatar.cc/300?img=44',
  'https://i.pravatar.cc/300?img=68',
  'https://i.pravatar.cc/300?img=32',
  'https://i.pravatar.cc/300?img=60',
  'https://i.pravatar.cc/300?img=51',
  'https://i.pravatar.cc/300?img=20',
  'https://i.pravatar.cc/300?img=49',
  'https://i.pravatar.cc/300?img=65',
  'https://i.pravatar.cc/300?img=57'
];

function openAvatarPicker() {
  var grid = document.getElementById('avatarGrid');
  grid.innerHTML = '';
  AVATAR_OPTIONS.forEach(function (url) {
    var img = document.createElement('img');
    img.src = url;
    img.loading = 'lazy';
    img.style.cssText = 'width:100%; aspect-ratio:1/1; border-radius:14px; background:rgba(255,255,255,0.06); border:2px solid rgba(255,255,255,0.08); cursor:pointer; padding:6px;';
    img.onclick = function () { selectAvatar(url); };
    grid.appendChild(img);
  });
  document.getElementById('avatarModal').style.display = 'flex';
}

function closeAvatarPicker() {
  document.getElementById('avatarModal').style.display = 'none';
}

function selectAvatar(url) {
  fetch('/profile/update-avatar', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ avatar: url })
  })
    .then(function (r) { return r.json(); })
    .then(function (data) {
      if (data.success) {
        var circle = document.getElementById('avatarCircle');
        circle.style.backgroundImage = "url('" + url + "')";
        circle.textContent = '';
        closeAvatarPicker();
        showToast('✅ প্রোফাইল ছবি পরিবর্তন হয়েছে!');
      } else {
        showToast('❌ আপডেট ব্যর্থ হয়েছে');
      }
    })
    .catch(function () { showToast('❌ সমস্যা হয়েছে'); });
}

function refreshBalance() {
  var icon = document.getElementById('refreshIcon');
  icon.classList.add('spinning');
  fetch('/profile/api/balance')
    .then(function (r) { return r.json(); })
    .then(function (data) {
      if (data.success) {
        document.getElementById('balanceText').textContent = '৳ ' + Number(data.coins).toLocaleString('<%= lang === "bn" ? "bn-BD" : "en-US" %>');
        var lu = document.getElementById('lastUpdatedText');
        if (lu) lu.textContent = 'এখনই';
        showToast('✅ ব্যালেন্স আপডেট হয়েছে');
      } else {
        showToast('❌ আপডেট ব্যর্থ হয়েছে');
      }
    })
    .catch(function () { showToast('❌ সমস্যা হয়েছে'); })
    .finally(function () {
      setTimeout(function () { icon.classList.remove('spinning'); }, 400);
    });
}
</script>

<a href="/payment/deposit" class="pf-sticky-deposit" title="দ্রুত ডিপোজিট"><i class="fas fa-plus"></i></a>

<%- include('../partials/navbar') %>
<%- include('../partials/bottom-nav') %>
