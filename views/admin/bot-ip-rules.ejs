<%
function escapeHtml(str) {
  return String(str == null ? '' : str)
    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
}
%>
<%- include('partials/admin-layout', {
    title: 'IP Block/Whitelist',
    active: 'bot-monitoring',
    body: `
<div class="max-w-4xl mx-auto">
  <div class="flex items-center justify-between mb-5">
    <h1 class="text-xl font-bold text-slate-800">🛡️ IP Block / Whitelist</h1>
    <a href="/admin/bot-monitoring" class="text-sm text-slate-500"><i class="fas fa-arrow-left"></i> ড্যাশবোর্ডে ফিরুন</a>
  </div>

  <div class="bg-white p-5 rounded-2xl border border-slate-100 shadow-sm mb-6">
    <p class="text-sm font-bold text-slate-700 mb-3">নতুন রুল যোগ করুন</p>
    <form method="POST" action="/admin/bot-monitoring/ip-rules" class="grid grid-cols-1 md:grid-cols-4 gap-2">
      <input type="text" name="ip" placeholder="IP অ্যাড্রেস (যেমন 103.21.244.10)" required class="border px-3 py-2.5 rounded-xl text-sm md:col-span-2">
      <select name="type" class="border px-3 py-2.5 rounded-xl text-sm">
        <option value="block">🚫 Block</option>
        <option value="whitelist">✅ Whitelist</option>
      </select>
      <button type="submit" class="px-4 py-2.5 bg-slate-800 text-white rounded-xl text-sm font-semibold">যোগ করুন</button>
      <input type="text" name="reason" placeholder="কারণ (ঐচ্ছিক)" class="border px-3 py-2.5 rounded-xl text-sm md:col-span-4">
    </form>
  </div>

  <div class="bg-white rounded-2xl border border-slate-100 shadow-sm overflow-hidden">
    <table class="w-full text-sm">
      <thead><tr class="text-left text-slate-400 text-xs uppercase">
        <th class="p-3">IP</th><th class="p-3">টাইপ</th><th class="p-3">কারণ</th><th class="p-3">যোগ করেছেন</th><th class="p-3">তারিখ</th><th class="p-3">অ্যাকশন</th>
      </tr></thead>
      <tbody>
        ${rules.map(r => `
          <tr class="border-t border-slate-50">
            <td class="p-3 font-mono">${escapeHtml(r.ip)}</td>
            <td class="p-3">${r.type === 'block' ? '<span class="text-red-500 font-bold">🚫 Block</span>' : '<span class="text-emerald-600 font-bold">✅ Whitelist</span>'}</td>
            <td class="p-3 text-slate-500">${escapeHtml(r.reason || '-')}</td>
            <td class="p-3 text-slate-500">${escapeHtml(r.created_by || '-')}</td>
            <td class="p-3 text-slate-500 whitespace-nowrap">${new Date(r.created_at).toLocaleString('en-US', { timeZone: 'Asia/Dhaka' })}</td>
            <td class="p-3">
              <form method="POST" action="/admin/bot-monitoring/ip-rules/${encodeURIComponent(r.ip)}/remove" onsubmit="return confirm('${escapeHtml(r.ip)}-এর রুল সরাবেন?')">
                <button type="submit" class="text-slate-400 hover:text-red-500 text-xs font-bold"><i class="fas fa-trash"></i> সরান</button>
              </form>
            </td>
          </tr>
        `).join('') || '<tr><td colspan="6" class="p-6 text-center text-slate-400">কোনো IP রুল নেই</td></tr>'}
      </tbody>
    </table>
  </div>
</div>
`
}) %>
