# PR_AUDIT.md

রিপোজিটরির **সব ৯২টি PR** (#1 – #92) পরীক্ষা করা হয়েছে।

| অবস্থা | সংখ্যা |
|---|---|
| মোট PR | 92 |
| Merged | 78 |
| বন্ধ, merge হয়নি | 13 |
| এখনো খোলা | 1 (#92 — অডিট বেসলাইন) |

গুরুত্বপূর্ণ: **কোনো পুরোনো PR ঝুলে নেই।** কাজেই বাকি সমস্যাগুলো "PR মার্জ হয়নি" বলে
নয় — সেগুলো কখনো ঠিকই করা হয়নি, অথবা পরে আবার ফিরে এসেছে।

---

## PR ইতিহাসের ভাগ

### ভাগ ক — UI / থিম / রিডিজাইন (৩৩টি PR)

`#1, 2, 3, 4, 10, 12, 17, 26, 29, 30, 31, 33, 34, 35, 37, 38, 40, 41, 42, 46, 47, 49,`
`51, 52, 54, 55, 56, 57, 58, 59, 60, 67, 68, 76, 87, 89, 90, 91`

সবচেয়ে বড় ভাগ। থিম/কনট্রাস্ট একই সমস্যা **অন্তত ৬ বার** ঠিক করা হয়েছে
(#33, #34, #54, #67, #68, #76, তারপর আবার #87–#91)। প্রতিবার ফিরে এসেছে, কারণ
টেমপ্লেটে ইনলাইন রঙ হার্ডকোড করা ছিল — মূল কারণ ধরা হয়নি, উপসর্গ ঠিক করা হয়েছে।

### ভাগ খ — নিরাপত্তা / আর্থিক শুদ্ধতা (১৪টি PR)

`#43, 44, 61, 62, 63, 64, 69, 70, 71, 74, 75, 79, 81, 82, 83, 84, 85, 86, 88`

সাম্প্রতিক কাজের মূল অংশ। CSPRNG, fail-closed গেট, cashout authority, ledger sign,
XSS/session/race, credential hardening।

### ভাগ গ — বহুভাষা / স্থানীয়করণ (৭টি PR)

`#4, 7, 13, 45, 48, 50, 53, 80`

BN/EN সিস্টেম তিনবার নতুন করে করা হয়েছে (#45, #48, #50/#53), পরে #80-তে
"Restore livo-i18n changes" — অর্থাৎ একবার হারিয়েও গিয়েছিল।

### ভাগ ঘ — গেম / স্পোর্টস কনটেন্ট (৯টি PR)

`#2, 8, 18, 22, 25, 28, 88`

১১০+ গেম যোগ করা হয়েছে, কিন্তু `routes/games.js`-এ handler নেই এমন গেম
জেনেরিক `45% win / 2x` fallback-এ চলে (ফাইন্ডিং #10)। অর্থাৎ কনটেন্ট PR গেম
লজিক PR-এর চেয়ে অনেক এগিয়ে গেছে।

### ভাগ ঙ — অবকাঠামো / ডিপ্লয় / DB (১১টি PR)

`#9, 11, 14, 15, 16, 24, 32, 36, 39, 65, 66, 72, 73, 77, 78`

সিনট্যাক্স এরর, স্টার্টআপ ক্র্যাশ, DB স্কিমা, কিউ সিস্টেম, Telegram ইন্টিগ্রেশন, E2E।

---

## PR ইতিহাস থেকে যা শেখা যায়

**১. একই সমস্যা বারবার ফিরে আসছে।** থিম ৬ বার, i18n ৪ বার, auth সিনট্যাক্স এরর
৪ বার (#9, #11, #14, #15)। কারণ প্রতিবার উপসর্গ ঠিক হয়েছে, মূল কারণ নয়।

**২. দুটো সিস্টেম পাশাপাশি চলছে।** #65 কিউ সিস্টেম যোগ করেছে, কিন্তু পুরোনো
`services/queue.js` থাকেনি — দুটোই সক্রিয় (ফাইন্ডিং #36)। একই কথা ব্যাকআপে
(#28) — `backup.js` আর `backupManager.js`।

**৩. রিভার্ট হয়েছে দুইবার** (#6, #57) — অর্থাৎ মার্জের পর সমস্যা ধরা পড়েছে।
CI-তে E2E গেট থাকলে এগুলো আগেই ধরা পড়ত (ফাইন্ডিং #50)।

---

## ৫৫টি ফাইন্ডিং → ৫ দফা কাজ

PR ইতিহাসে যেসব কাজ একসাথে হয়েছে, একই ফাইল ছুঁয়েছে, বা একই মূল কারণ থেকে
এসেছে — সেগুলো একত্র করে ৫ দফা:

### দফা ১ — ডেটা এক্সপোজার ও ব্যাকআপ
**ফাইন্ডিং:** 1, 27, 28, 29, 30, 24, 26, 3

সব একই মূল কারণ: সংবেদনশীল ডেটা এমন জায়গায় যাচ্ছে যেখানে যাওয়ার কথা নয়।
ফাইল: `services/backup.js`, `services/backupManager.js`, KYC আপলোড পথ, `.gitignore`।
একসাথে করলে একবারই এনক্রিপশন ইউটিলিটি লিখতে হবে — TOTP, KYC, ব্যাকআপ সবাই ব্যবহার করবে।

### দফা ২ — প্রমাণীকরণ ও টোকেন
**ফাইন্ডিং:** 2, 4, 5, 22, 23, 25, 44, 53

সব `middleware/auth.js`, `routes/auth.js`, `db.js`, `head.ejs` — অল্প কয়েকটা ফাইল।
Host header, টোকেন হ্যাশিং, fail-closed, TLS, CSRF scope, age gate — একই
"বিশ্বাস কোথা থেকে আসে" প্রশ্ন।

### দফা ৩ — টাকা ও লেজার
**ফাইন্ডিং:** 8, 9(হয়ে গেছে), 10, 11, 12, 13, 14, 15, 16, 17, 18, 39, 40, 41

সবচেয়ে বড় দফা, কিন্তু সব `routes/games.js`, `routes/payment.js`, `routes/matches.js`
আর `services/`-এর রিওয়ার্ড ফাইলগুলোতে। ১৫–১৮ (loyalty/VIP/streak atomicity)
একই প্যাটার্ন — একটাই transaction helper লিখলে সবগুলোই ঠিক হবে।

### দফা ৪ — স্টেট ও ব্যাকগ্রাউন্ড কাজ
**ফাইন্ডিং:** 19, 20, 21, 31, 32, 33, 34, 35, 36, 37, 38, 42, 43, 48

KYC স্টেট মেশিন, মাইগ্রেশন, শিডিউলার, কিউ, টাইমজোন, service worker।
সব "একই কাজ দুবার হচ্ছে বা ভুল সময়ে হচ্ছে" — এক ধরনের সমস্যা।

### দফা ৫ — সততা ও CI
**ফাইন্ডিং:** 45, 46, 47, 49, 50, 51, 52, 54, 55, 6, 7

CSP, ভুয়া দাবি, simulated SMS, fraud scan যা চলে না, CI/E2E/coverage,
Telegram GitHub write। সব "যা দেখাচ্ছে আর যা আসলে হচ্ছে — দুটো এক নয়"।

---

## সুপারিশ

দফা ১ → ২ → ৩ → ৪ → ৫ ক্রমে। প্রতিটি দফা একটি PR, ভেতরে আলাদা কমিট,
প্রতিটি ফিক্সের সাথে regression test।

দফা ৩ সবচেয়ে বড় — প্রয়োজনে দুই ভাগে (গেম/লেজার এবং রিওয়ার্ড atomicity)।
