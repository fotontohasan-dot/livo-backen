# হ্যান্ডওভার — `fix/audit-phases-0-4`

**বেস:** `869d13d` · **কমিট সংখ্যা:** ৫২ · **তারিখ:** ২০২৬-০৯-০৮

এই নথিটা অডিট রিপোর্টের কাজ শেষ করার পর লেখা। উদ্দেশ্য: কী হয়েছে, কী
বাকি, আর কোন সিদ্ধান্তগুলো মালিকের নেওয়া দরকার — সব এক জায়গায়।

---

## ১. 🔴 সবার আগে যেটা করতে হবে

**`bank_cards.holder_name` কলামে ব্যবহারকারীদের আসল পাসওয়ার্ড প্লেইনটেক্সটে
জমা আছে।**

`views/profile/cards.ejs`-এ ওয়ালেট যোগ করার ফর্মে একটা ফিল্ড ছিল যার লেবেল
"পাসওয়ার্ড", `type="password"`, placeholder "আপনার পাসওয়ার্ড লিখুন" — কিন্তু
তার `name="holder_name"`। `routes/profile.js` সেটাকে সরাসরি
`bank_cards.holder_name` কলামে জমা করত, কোনো হ্যাশ ছাড়া, আর কার্ড তালিকায়
"হোল্ডারের নাম" হিসেবে দেখাতও।

ভিউ ঠিক করা হয়েছে (কমিট `64466d7`), **কিন্তু কোডের ফিক্স পুরনো ডেটা মোছে না।**

করণীয়:
1. `SELECT id, user_id, holder_name FROM bank_cards;` — যেসব মান নামের মতো নয়
   সেগুলো সম্ভবত পাসওয়ার্ড
2. ওই সারিগুলোর `holder_name` মুছুন বা খালি করুন
3. আক্রান্ত ব্যবহারকারীদের পাসওয়ার্ড রিসেট করান

মাইগ্রেশন ইচ্ছাকৃতভাবে লেখা হয়নি — কোনটা নাম আর কোনটা পাসওয়ার্ড, সেটা
আন্দাজে ঠিক করলে আসল ডেটা মুছে যেতে পারে। সিদ্ধান্তটা মানুষের।

---

## ২. মালিকের সিদ্ধান্ত দরকার

| বিষয় | এখন কী আছে | কেন সিদ্ধান্ত দরকার |
|---|---|---|
| রেফারেল টার্নওভার | `referral: { sports: 3, casino: 0 }` | সংখ্যাটা আমার অনুমান। ব্যবসায়িক যুক্তিতে বদলাতে পারে। `services/turnover.js` |
| উইথড্র সিলিং | per-request ৫০,০০০ · per-day ১,০০,০০০ | নিরাপদ ডিফল্ট বসানো হয়েছে। অ্যাডমিন প্যানেল থেকে বদলানো যায় (`site_settings`) |
| Next.js মৃত কোড | `src/`, `next.config.js` ইত্যাদি অক্ষত | আপনি "কিছু মুছবে না" বলেছেন, তাই রাখা হয়েছে। মুছবেন না আলাদা রিপোতে সরাবেন? |
| জুয়া-আসক্তি হেল্পলাইন | কোনো নম্বর নেই | বাংলাদেশে জাতীয় হেল্পলাইন আছে কিনা যাচাই করতে পারিনি। ভুল নম্বর দেওয়া নম্বর না দেওয়ার চেয়ে খারাপ। যাচাই করে `views/profile/responsible.ejs`-এর চিহ্নিত জায়গায় বসান |

---

## ৩. যা এই পরিবেশে করা যায়নি

- **ভিডিও ট্রান্সকোড** — ffmpeg নেটওয়ার্ক অ্যালাউলিস্টে নেই।
  `public/videos/jackpot-girl.mp4` (৩.৯MB) → WebM/MP4 ২৬০px।
  ভিউতে `<source>` ট্যাগ ও `preload="none"` ইতিমধ্যে বসানো আছে, শুধু
  `public/videos/jackpot-girl.webm` ফাইলটা তৈরি করলেই কাজ করবে।
- **কভারেজ ratchet** — পূর্ণ কভারেজ রান এখানে সম্ভব হয়নি (১৫ মিনিটে ৯ সুইট)।
  চারটে আর্থিক ফাইলে মাপা (আংশিক সুইট থেকে, তাই এগুলো সর্বনিম্ন সীমা):

  | ফাইল | statements |
  |---|---|
  | `routes/payment.js` | ৩৬.৫% |
  | `services/turnover.js` | ৩৯% |
  | `routes/games.js` | ৪৮% |
  | `services/referral.js` | ৭৫% |

  ⚠️ অডিট ধরে নিয়েছিল কভারেজ ২৫/১৫ গেটের অনেক উপরে। **সেটা ভুল।**
  ৮০% থ্রেশহোল্ড এখনই বসালে CI ভাঙবে।

---

## ৪. মার্জের আগে দেখুন

- **স্পোর্টস বেট সেটেলমেন্ট** (`routes/matches.js`, `services/accumulator.js`,
  `services/oddsResolver.js`) — এখানে `addTurnover` কল ট্রানজেকশনের ভেতরে
  সরানো হয়েছে, **কিন্তু আশপাশের সেটেলমেন্ট লজিক আমি পড়িনি।** টেস্ট পাস
  করছে, কিন্তু না-পড়া কোডে টেস্ট পাস করা দুর্বল প্রমাণ। অডিটেও বলা ছিল
  পরের অডিট এখান থেকে শুরু করতে।
- **`pending_review` রেফারেল বোনাস** — duplicate-account সন্দেহ হলে বোনাস
  এই অবস্থায় তৈরি হয় ও কয়েন আটকে থাকে, কিন্তু **অ্যাডমিন অনুমোদনের UI
  এখনো নেই।** ওটা না বানানো পর্যন্ত ফ্ল্যাগড বোনাস চিরকাল আটকে থাকবে।

---

## ৫. টেস্টের অবস্থা

সবুজ: render (৪৩ সুইট / ১২৪১), আর অন্য ব্যাচগুলোর ২,৭০০+ টেস্ট।

**তিনটে ব্যাকআপ-সংক্রান্ত টেস্ট এই স্যান্ডবক্সে ব্যর্থ:**

| টেস্ট | কারণ |
|---|---|
| `tests/integration/restorePerformance.test.js` | SIGKILL (OOM) |
| `tests/backup-restore.test.js` | SIGTERM/SIGKILL |
| `tests/integration/backupAlerting.test.js` | ৩০s টাইমআউট (pg_dump ধীর) |

**তিনটেই পরিষ্কার `869d13d` বেসলাইনেও একইভাবে ব্যর্থ হয়** — যাচাই করা হয়েছে।
অর্থাৎ এই ব্রাঞ্চের কারণে নয়; স্যান্ডবক্সের ৪GB RAM ও ধীর ডিস্কের ফল।
আপনার CI-তে চালিয়ে নিশ্চিত হয়ে নিন।

---

## ৬. অবশিষ্ট i18n

| অংশ | শুরুতে | এখন |
|---|---|---|
| ইউজার-ফেসিং | ৫৩২ | ৩৯ |
| অ্যাডমিন | ৬৭৩ | ২২ |

অবশিষ্ট ৬১ লাইনের বেশিরভাগ তিনটে ফাইলে, যেগুলো **সুযোগের বাইরে** বলে
ছোঁয়া হয়নি: `views/profile/contest.ejs`,
`views/partials/tournament-showcase.ejs`, `views/admin/leaderboard.ejs`।

`tests/render/i18nRatchet.test.js` নিশ্চিত করে সংখ্যা আর বাড়তে পারবে না।

⚠️ `views/profile/contest.ejs`-এ বেসলাইন থেকেই নষ্ট বানান আছে —
"রেফারেল কনটেস্" (ট বাদ), "প্রইজ", "লিডারবোর্ "। লিডারবোর্ডের কাজ শুরু
করলে এগুলোও ঠিক করবেন।

---

## ৭. পথে পাওয়া ত্রুটি যা অডিটে ছিল না

1. `bank_cards.holder_name`-এ প্লেইনটেক্সট পাসওয়ার্ড (উপরে §১)
2. `t` ভ্যারিয়েবল শ্যাডোয়িং — `periodic.ejs`, `notification-templates.ejs`
   (দুটোতেই লুপ ভ্যারিয়েবলের নাম `t`, অনুবাদক ঢেকে যেত)
3. `deposit.ejs`-এ `t.channel_1 + '০'` → ইংরেজিতে "Channel 1০" মিশ্র-লিপি
4. নষ্ট বাংলা বানান — `freebet.ejs` ("স্পর্টস ৫x / ক্যাসিন ৩৫x" — টার্নওভার
   শর্তের ব্যাখ্যা, অর্থাৎ আর্থিক তথ্য), `streak.ejs`, `missions.ejs`
5. `responsible.ejs`-এর সাপোর্ট লিংক `/support`-এ যেত, যেটা অ্যাডমিন-অনলি
   রুট; সঠিক পথ `/profile/support`
6. `head.ejs`-এর SEO মেটা ডিফল্ট বাংলা — ইংরেজি পাতার title/description
   সার্চ ইঞ্জিনেও বাংলায় যেত

---

## ৮. আমার একটা ভুল — রেকর্ডের জন্য

কমিট `7a73863` ও `9c09167`-এ দাবি করেছিলাম `t.deposit` একটা বাগ ("অবজেক্ট
অ্যাক্সেস, কী রিজলভ হয় না")। **দাবিটা ভুল ছিল।** `app.js:607`-এ
`res.locals.t` একটা Proxy যার `get` ট্র্যাপ আছে, আর পাশের কমেন্টেই লেখা
"Proxy allow both t('key') and t.key"। ভিউতে এই ফর্ম ৭১০ জায়গায় ব্যবহৃত —
ইচ্ছাকৃত নকশা।

কোড পরিবর্তন ক্ষতিকর নয় (দুটো ফর্ম সমতুল্য), কিন্তু কমিট বার্তার ভুল
রেকর্ড রেখে গেলে কেউ পরে ৭১০টা ব্যবহার "ঠিক করতে" যেতে পারত। সংশোধন
কমিট `5604f0c`-এ লেখা আছে।

---

## ৯. পুশ

ব্রাঞ্চটা এখনো পুশ করা হয়নি (এই পরিবেশে GitHub ক্রেডেনশিয়াল নেই):

```
git push -u origin fix/audit-phases-0-4
```
