# Livo — অবশিষ্ট কাজের নির্দেশিকা (দ্বিতীয় পর্ব)

**রিপো:** `fotontohasan-dot/livo-backen`
**ভিত্তি ব্রাঞ্চ:** `fix/audit-phases-0-4` (৫২ কমিট, বেস `869d13d`)
**তারিখ:** ২০২৬-০৯-০৮

> **পড়ার আগে:** প্রথম পর্বের অডিট-নির্দেশিকার ধাপ ০ থেকে ৪ ইতিমধ্যে শেষ।
> এই নথি শুধু **অবশিষ্ট** কাজের। ব্রাঞ্চে `HANDOVER.md` ফাইলে বিস্তারিত
> প্রেক্ষাপট আছে — কাজ শুরুর আগে সেটা পড়ুন।

---

## গুরুত্বপূর্ণ প্রেক্ষাপট (না জানলে ভুল হবে)

**১. `t` একটা Proxy, ফাংশনও, অবজেক্টও।**
`app.js:607`-এ:
```js
res.locals.t = new Proxy(t_func, {
  get: (target, prop) => translations[lang][prop] || prop
});
```
অর্থাৎ `t('key')` আর `t.key` — **দুটোই সঠিক ও সমতুল্য**। কোডবেসে `t.key`
ফর্ম ৭১০ জায়গায় ব্যবহৃত। **এটা বাগ নয়, ইচ্ছাকৃত নকশা। "ঠিক করতে" যাবেন না।**

**২. অ্যাডমিন ভিউয়ের গঠন আলাদা।**
বেশিরভাগ অ্যাডমিন ভিউ `admin-layout`-এ পাঠানো একটা সার্ভার-সাইড template
literal (`` body: `...` ``)-এর ভেতরে। সেখানে `t()` পাওয়া যায়, কিন্তু:
- template literal-এর ভেতরে → `${t('key')}`
- ওই literal-এর ভেতরে আবার সিঙ্গল-কোটেড JS স্ট্রিং কনক্যাটেনেশন থাকলে →
  `' + t("key") + '` (নাহলে কোট স্ট্রিং ভেঙে দেয়, কম্পাইল এরর হয়)

**৩. `t` নামে লুপ ভ্যারিয়েবল থাকলে অনুবাদক ঢাকা পড়ে।**
`periodic.ejs` ও `notification-templates.ejs`-এ এই সমস্যা ছিল, ঠিক করা
হয়েছে। নতুন ফাইলে `forEach(t => ...)` দেখলে আগে নাম বদলান।

**৪. ৳ চিহ্ন অনুবাদ করার জিনিস নয়** — ইংরেজি পাতাতেও ৳ থাকবে।
`tests/render/i18nRatchet.test.js` এটা গণনা থেকে বাদ দেয়।

**৫. পরিবেশগত টেস্ট ব্যর্থতা।**
`restorePerformance`, `backup-restore`, `backupAlerting` — এই তিনটে কম
মেমরি/ধীর ডিস্কের পরিবেশে ব্যর্থ হয়। **পরিষ্কার `869d13d` বেসলাইনেও একই
ফল** (যাচাই করা)। এগুলো ঠিক করার চেষ্টা করবেন না; ব্যর্থ হলে আগে বেসলাইনে
চালিয়ে দেখুন।

---

## কাজের ক্রম

| ধাপ | কাজ | কেন এই ক্রম |
|---|---|---|
| ধাপ ১ | ডেটা পরিষ্কার (পাসওয়ার্ড ফাঁস) | সক্রিয় নিরাপত্তা ঝুঁকি |
| ধাপ ২ | `pending_review` বোনাস UI | ব্যবহারকারীর টাকা আটকে আছে |
| ধাপ ৩ | স্পোর্টস সেটেলমেন্ট অডিট | না-পড়া আর্থিক কোড |
| ধাপ ৪ | কভারেজ ratchet | রিগ্রেশন সুরক্ষা |
| ধাপ ৫ | ভিডিও ট্রান্সকোড | পারফরম্যান্স |
| ধাপ ৬ | অবশিষ্ট i18n | পরিচ্ছন্নতা |

প্রতিটা ধাপ শেষে টেস্ট চালিয়ে কমিট করুন। একসাথে সব করবেন না।

---

# ধাপ ১ — 🔴 `bank_cards`-এ প্লেইনটেক্সট পাসওয়ার্ড (সর্বোচ্চ অগ্রাধিকার)

## সমস্যা

`views/profile/cards.ejs`-এ ওয়ালেট যোগ করার ফর্মে একটা ফিল্ড ছিল:
- লেবেল: "পাসওয়ার্ড"
- `type="password"`
- placeholder: "আপনার পাসওয়ার্ড লিখুন"
- **কিন্তু** `name="holder_name"`

`routes/profile.js` এটাকে সরাসরি `bank_cards.holder_name` কলামে জমা করত,
কোনো হ্যাশ ছাড়া, এবং কার্ড তালিকায় "হোল্ডারের নাম" হিসেবে দেখাতও।

ভিউ ঠিক করা হয়েছে (কমিট `64466d7`) — কিন্তু **পুরনো ডেটা রয়ে গেছে।**

## করণীয়

⚠️ **এটা ডেটা-ধ্বংসাত্মক কাজ। কোনো স্বয়ংক্রিয় মাইগ্রেশন লিখবেন না।**
কোনটা আসল নাম আর কোনটা পাসওয়ার্ড — সেটা heuristic দিয়ে ঠিক করা বিপজ্জনক।

1. আগে **শুধু পড়ুন**, কিছু বদলাবেন না:
   ```sql
   SELECT id, user_id, holder_name, created_at
   FROM bank_cards
   ORDER BY created_at DESC;
   ```
2. মানুষের চোখে দেখে আলাদা করুন কোনগুলো নামের মতো নয়
3. সন্দেহজনক সারিগুলোর তালিকা মালিককে দেখান — **অনুমোদন ছাড়া মুছবেন না**
4. অনুমোদনের পর ওই সারিগুলোর `holder_name` খালি করুন
5. আক্রান্ত `user_id`-গুলোর তালিকা দিন যাতে মালিক পাসওয়ার্ড রিসেট করাতে পারেন

## টেস্ট

`tests/security/`-এ নতুন ফাইল `cardsFieldIntegrity.test.js`:
- ওয়ালেট যোগ করার ফর্মে `type="password"` ফিল্ড **নেই** তা যাচাই
- `holder_name` ফিল্ডে নাম পাঠালে সেটাই সেভ হয়
- (ratchet হিসেবে) `views/profile/cards.ejs`-এ `type="password"` স্ট্রিং
  আর কখনো ফিরে না আসে

---

# ধাপ ২ — `pending_review` রেফারেল বোনাসের অ্যাডমিন UI

## প্রেক্ষাপট

`services/referral.js` এখন পেআউটের আগে `evaluateDuplicateAccount()` চালায়।
`risk_score >= 50` হলে:
- `bonuses` সারি তৈরি হয় `status = 'pending_review'` নিয়ে
- **কয়েন দেওয়া হয় না**
- ব্যবহারকারী নোটিফিকেশন পান "যাচাইয়ের জন্য অপেক্ষমাণ"

**কিন্তু অনুমোদনের কোনো UI নেই** — ফলে ফ্ল্যাগড বোনাস চিরকাল আটকে থাকবে।

## করণীয়

1. নতুন অ্যাডমিন পেজ `/admin/pending-bonuses` (রুট + ভিউ)
   - `SELECT * FROM bonuses WHERE status = 'pending_review'`
   - প্রতিটার সাথে ইউজার, পরিমাণ, তৈরির সময়, আর সংশ্লিষ্ট
     `duplicate_flags` সারি (risk_score ও কারণ) দেখান
2. দুটো অ্যাকশন:
   - **অনুমোদন** → `status = 'active'` করুন **এবং** কয়েন যোগ করুন
     (একই ট্রানজেকশনে, `coin_transactions`-এ এন্ট্রিসহ)
   - **বাতিল** → `status = 'rejected'`, কয়েন দেওয়া হয় না
3. দুটো অ্যাকশনেই `data-confirm` ও `auditLog` এন্ট্রি
4. অ্যাডমিন নেভিগেশনে লিংক যোগ (`utils/adminNav.js`)
5. RBAC পারমিশন — বিদ্যমান `payment_manage` বা অনুরূপ ব্যবহার করুন

⚠️ কয়েন যোগ করার সময় **অবশ্যই** ইউজার সারিতে `FOR UPDATE` লক নিন —
`routes/payment.js`-এর `creditApprovedDeposit()` দেখুন, সেখানে একই প্যাটার্ন।

## টেস্ট

`tests/security/pendingBonusApproval.test.js`:
- pending বোনাস অনুমোদনে কয়েন যোগ হয়, status 'active' হয়
- বাতিলে কয়েন যোগ হয় **না**
- একই বোনাস দুবার অনুমোদন করা যায় না (ডাবল-ক্রেডিট গার্ড)
- পারমিশন ছাড়া অ্যাডমিন ৪০৩ পায়

---

# ধাপ ৩ — স্পোর্টস বেট সেটেলমেন্ট অডিট

## কেন

প্রথম পর্বে `addTurnover()` কল তিন জায়গায় ট্রানজেকশনের ভেতরে সরানো হয়েছে:
- `routes/games.js` (২টি কলসাইট)
- `routes/matches.js`
- `services/accumulator.js`

**কিন্তু আশপাশের সেটেলমেন্ট লজিক পড়া হয়নি।** টেস্ট পাস করছে, কিন্তু
না-পড়া কোডে টেস্ট পাস করা দুর্বল প্রমাণ। মূল অডিটেও বলা ছিল পরের অডিট
এখান থেকে শুরু করতে।

## করণীয়

`routes/matches.js`, `services/accumulator.js`, `services/oddsResolver.js`
পড়ুন এবং নিচের প্রশ্নগুলোর উত্তর খুঁজুন:

1. **ডাবল-সেটেলমেন্ট সম্ভব?** একই বেট দুবার সেটেল হলে কি দুবার পেআউট হয়?
   সেটেলমেন্টে `status` চেক ও row lock আছে কি?
2. **অড পরিবর্তন** — বেট রাখার পর অ্যাডমিন অড বদলালে কোন অড দিয়ে সেটেল হয়?
   বেটের সাথে অড snapshot সংরক্ষিত হয়, নাকি লাইভ অড পড়া হয়?
3. **accumulator (মাল্টি-বেট)** — একটা লেগ বাতিল হলে কী হয়? পুরো
   accumulator void হয়, নাকি ওই লেগ ১.০ অড ধরে বাকিটা চলে?
4. **পেআউট ট্রানজেকশন** — কয়েন যোগ ও `bets.status` আপডেট একই
   ট্রানজেকশনে কিনা? মাঝপথে ব্যর্থ হলে কী হয়?
5. **`addTurnover` এখন ট্রানজেকশনের ভেতরে** — সেটেলমেন্ট পাথে কোনো
   দীর্ঘ ট্রানজেকশন তৈরি হচ্ছে কিনা (লক কনটেনশন)?

যা পাবেন তা রিপোর্ট করুন। **ফিক্স লেখার আগে মালিকের অনুমোদন নিন** —
এটা মূল পণ্যের আর্থিক কোড।

---

# ধাপ ৪ — কভারেজ ratchet

## প্রেক্ষাপট

`jest.config.js`-এ থ্রেশহোল্ড এখন `statements: 25, branches: 15`।

মূল অডিট ধরে নিয়েছিল আসল কভারেজ এর অনেক উপরে। **সেটা ভুল প্রমাণিত।**
আংশিক সুইট থেকে মাপা (সর্বনিম্ন সীমা):

| ফাইল | statements |
|---|---|
| `routes/payment.js` | ৩৬.৫% |
| `services/turnover.js` | ৩৯% |
| `routes/games.js` | ৪৮% |
| `services/referral.js` | ৭৫% |

## করণীয়

1. পূর্ণ কভারেজ রান করুন (সময় লাগবে, ধৈর্য রাখুন):
   ```
   npm run test:coverage
   ```
2. আসল গ্লোবাল সংখ্যা দেখে `jest.config.js`-এর থ্রেশহোল্ড **ঠিক সেখানে**
   সেট করুন (২-৩% মার্জিন রেখে)
3. চারটে আর্থিক ফাইলের জন্য আলাদা (উচ্চতর) থ্রেশহোল্ড দিন —
   কিন্তু **মাপা সংখ্যার উপর ভিত্তি করে**, অনুমানে নয়

⚠️ **৮০% বসাবেন না।** মূল অডিটে ৮০%+ সুপারিশ ছিল, কিন্তু বাস্তব কভারেজ
অনেক নিচে — বসালেই CI ভাঙবে। ratchet মানে "আর কমতে পারবে না", "এখনই
লক্ষ্যে পৌঁছাতে হবে" নয়।

---

# ধাপ ৫ — ভিডিও ট্রান্সকোড

## প্রেক্ষাপট

`public/videos/jackpot-girl.mp4` = ৩.৯MB, `public/`-এর ৭৫% ওজন।
দেখানো হয় ১৩০px চওড়ায়।

ভিউতে (`views/index.ejs`) কাজ **ইতিমধ্যে করা**:
```html
<video ... preload="none">
  <source src="/videos/jackpot-girl.webm" type="video/webm">
  <source src="/videos/jackpot-girl.mp4" type="video/mp4">
</video>
```

শুধু WebM ফাইলটা নেই।

## করণীয়

```bash
ffmpeg -i public/videos/jackpot-girl.mp4 \
  -vf "scale=260:-2" -c:v libvpx-vp9 -crf 40 -b:v 0 -an \
  public/videos/jackpot-girl.webm
```

লক্ষ্য: **৫০–১৫০KB**। এর বেশি হলে `-crf` বাড়ান।
তৈরি হলে ফাইল সাইজ যাচাই করে কমিট করুন।

---

# ধাপ ৬ — অবশিষ্ট i18n (~৬১ লাইন)

## অবস্থা

| অংশ | শুরুতে | এখন |
|---|---|---|
| ইউজার-ফেসিং | ৫৩২ | ৩৯ |
| অ্যাডমিন | ৬৭৩ | ২২ |

`tests/render/i18nRatchet.test.js` নিশ্চিত করে সংখ্যা আর বাড়তে পারবে না।
কমালে baseline-ও কমাতে হবে (টেস্ট নিজেই মনে করিয়ে দেয়)।

## অবশিষ্ট ফাইল

তিনটে, এবং **তিনটেই প্রথম পর্বে ইচ্ছাকৃতভাবে বাদ রাখা হয়েছিল** কারণ
মালিক লিডারবোর্ড ও টুর্নামেন্ট শোকেসের কাজ পরে করতে চেয়েছিলেন:

- `views/profile/contest.ejs` (১৭ লাইন)
- `views/partials/tournament-showcase.ejs` (১৬ লাইন)
- `views/admin/leaderboard.ejs` (৬ লাইন)

**মালিকের অনুমতি নিয়ে তবেই ধরবেন।**

## অতিরিক্ত: নষ্ট বানান

`views/profile/contest.ejs`-এ বেসলাইন থেকেই অক্ষর-হারানো টেক্সট আছে:
- "রেফারেল কনটেস্" → "কনটেস্ট"
- "প্রইজ" → "প্রাইজ"
- "লিডারবোর্ " → "লিডারবোর্ড"

(একই ধরনের ক্ষয় `freebet.ejs`, `streak.ejs`, `missions.ejs`-এ ছিল, ঠিক
করা হয়েছে। অনুবাদের সময় সঠিক বানান locale কী-তে বসান।)

---

# কাজের নিয়ম (Ground rules)

1. **এক PR = এক ধাপ।**
2. **প্রতিটা ফিক্সের সাথে টেস্ট।**
3. **ধ্বংসাত্মক কাজে (ধাপ ১) মালিকের অনুমোদন ছাড়া এগোবেন না।**
4. **আর্থিক কোডে (ধাপ ২, ৩) ফিক্স লেখার আগে রিপোর্ট করুন।**
5. **DB মাইগ্রেশন সবসময় `IF NOT EXISTS` দিয়ে।**
6. **প্রতিটা ধাপ শেষে** `npx jest tests/render/` সবুজ হতে হবে, আর সম্ভব হলে
   পুরো সুইট।
7. **টেস্ট ব্যর্থ হলে আগে বেসলাইনে (`869d13d`) চালিয়ে দেখুন** — পরিবেশগত
   কিনা নিশ্চিত হন, তারপর কোডে হাত দিন।
8. **যা যাচাই করেননি তা "কাজ করে" বলবেন না।** টেস্ট চালিয়ে ফল দেখান।

---

# যা এখনো অডিট করা হয়নি

সততার খাতিরে — নিচের অংশগুলো দুই পর্বের কোনোটাতেই পড়া হয়নি:

- `routes/admin.js` (~৪৫০০ লাইন) — IDOR ও পারমিশন ফাঁক
  *(তবে `adminAuthorizationMatrix.test.js`,
  `adminRoutePermissionSweep.test.js`, `privilegeEscalationGuard.test.js`
  আছে, যা ভালো লক্ষণ)*
- `services/wheel.js`, `redpacket.js`, `dailyReward.js`, `missions.js`,
  `cashback.js`, `vip.js`, `streak.js` — সব কয়েন-প্রদানকারী ফিচার
- `telegram-bot.js` (৩৩KB) — ওয়েবহুক সিক্রেট, কমান্ড অথরাইজেশন
- `migrations.js` (১২৮KB) — কনস্ট্রেইন্ট, ফরেন কী, ক্যাসকেড
- **আসল রানটাইম আচরণ** — সাইট ব্রাউজারে চালানো হয়নি, মোবাইল UX দেখা হয়নি
